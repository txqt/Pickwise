# Use Avalonia and .NET for the desktop shell

We will build the Companion Shell with Avalonia and .NET. The first release is Windows-only, but Avalonia keeps a practical path to macOS/Linux later without committing to Electron's JavaScript stack or WPF's Windows lock-in.
