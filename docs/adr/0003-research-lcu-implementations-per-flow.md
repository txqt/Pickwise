# Research LCU implementations per flow

We will research existing League Client integrations for each required MVP flow before implementing it. We will copy or adapt the smallest license-compatible pieces when useful, but avoid forking a full companion app unless it already matches the MVP shape.
